import { n as findLocation, t as LOCATIONS } from "./locations-Crohst2h.js";
import { createFileRoute, lazyRouteComponent, notFound } from "@tanstack/react-router";
//#region src/routes/locations.$location.tsx
var $$splitComponentImporter = () => import("./locations._location-CJBS6-4b.js");
var Route = createFileRoute("/locations/$location")({
	loader: ({ params }) => {
		const loc = findLocation(params.location);
		if (!loc) throw notFound();
		return { loc };
	},
	component: lazyRouteComponent($$splitComponentImporter, "component"),
	head: ({ loaderData }) => {
		if (!loaderData) return { meta: [{ title: "Location not found — Her Fitness" }, {
			name: "robots",
			content: "noindex"
		}] };
		const loc = loaderData.loc;
		return {
			meta: [
				{ title: `Women-Only Gym in ${loc.name} | Her Fitness ${loc.name}` },
				{
					name: "description",
					content: `Her Fitness ${loc.name} is ${loc.area}'s premium women-only gym. Strength, yoga, dance & HIIT led by certified female & male fitness coaches. Serving ${loc.serves.slice(0, 3).join(", ")}. Free 3-day trial.`
				},
				{
					name: "keywords",
					content: `women only gym ${loc.name}, ladies gym ${loc.name}, female fitness ${loc.name}, gym for women near ${loc.name}, zumba ${loc.name}, yoga classes ${loc.name}, personal trainer for women ${loc.name}, best gym for women in ${loc.name}`
				},
				{
					property: "og:title",
					content: `Her Fitness ${loc.name} — Women-Only Gym in ${loc.area}`
				},
				{
					property: "og:description",
					content: loc.intro
				},
				{
					property: "og:type",
					content: "website"
				},
				{
					property: "og:url",
					content: `/locations/${loc.slug}`
				},
				{
					name: "twitter:card",
					content: "summary_large_image"
				},
				{
					name: "twitter:title",
					content: `Her Fitness ${loc.name}`
				},
				{
					name: "twitter:description",
					content: loc.intro
				},
				{
					name: "geo.region",
					content: "IN-DL"
				},
				{
					name: "geo.placename",
					content: `${loc.name}, Delhi`
				}
			],
			links: [{
				rel: "canonical",
				href: `/locations/${loc.slug}`
			}],
			scripts: [{
				type: "application/ld+json",
				children: JSON.stringify({
					"@context": "https://schema.org",
					"@type": "HealthClub",
					name: `Her Fitness ${loc.name}`,
					description: loc.intro,
					telephone: loc.phone,
					address: {
						"@type": "PostalAddress",
						streetAddress: loc.address,
						addressLocality: loc.name,
						addressRegion: "Delhi",
						addressCountry: "IN"
					},
					areaServed: loc.serves,
					openingHours: "Mo-Su 06:00-22:00",
					aggregateRating: {
						"@type": "AggregateRating",
						ratingValue: "4.9",
						reviewCount: "180"
					}
				})
			}, {
				type: "application/ld+json",
				children: JSON.stringify({
					"@context": "https://schema.org",
					"@type": "FAQPage",
					mainEntity: loc.faqs.map((f) => ({
						"@type": "Question",
						name: f.q,
						acceptedAnswer: {
							"@type": "Answer",
							text: f.a
						}
					}))
				})
			}]
		};
	},
	staticData: { prerender: LOCATIONS.map((l) => ({ location: l.slug })) }
});
//#endregion
export { Route as t };
